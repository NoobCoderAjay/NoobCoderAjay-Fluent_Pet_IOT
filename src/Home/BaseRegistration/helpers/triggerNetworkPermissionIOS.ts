// import { Platform } from 'react-native';

// import { getConnectionStatus } from 'src/api/base';
// import { IS_IOS } from 'src/constants';

// /**
//   Dummy request to trigger the local network permission prompt in iOS 14+
//  */

// const triggerNetworkPermissionIOS = () => {
//   const majorVersionIOS = IS_IOS && parseInt(Platform.Version.toString(), 10);

//   if (majorVersionIOS && majorVersionIOS >= 14) {
//     getConnectionStatus();
//   }
// };

// export default triggerNetworkPermissionIOS;
